import { describe, expect, it } from "vitest"; import { readFileSync } from "node:fs";
describe("Sprint 17 mobile analytics scope",()=>{it("gives Owner Mobile real analytics routes",()=>{const owner=readFileSync("apps/owner-mobile/app/[screen].tsx","utf8");expect(owner).toContain("/v1/analytics/command-center");expect(owner).toContain("/v1/analytics/branches/compare");});it("limits Staff Mobile to personal performance",()=>{const staff=readFileSync("apps/staff-mobile/app/[screen].tsx","utf8");expect(staff).toContain("/v1/analytics/staff/me");expect(staff).not.toContain("/v1/analytics/finance");});});
