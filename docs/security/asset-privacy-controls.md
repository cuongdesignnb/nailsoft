# Asset privacy controls

Every query is tenant-scoped and operational mutations re-check persisted branch scope. Documents/photos use private storage keys and signed URLs. Asset codes, serials and vendor information never cross tenant scope. Physical deletion is not exposed after capitalization; CSV exports must sanitize formula-leading cells.
