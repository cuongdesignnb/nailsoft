# Technical debt register

| Item                                           | Status                        | Impact                                                                                                                                                                                                                     | Follow-up                                                                                                                                                     |
| ---------------------------------------------- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Production OTP vendor and credentials          | Release blocker               | Durable encrypted webhook delivery is implemented, but the production vendor/account is not selected                                                                                                                       | Owner: Platform/SecOps; environment: pre-production; deadline: before production go/no-go; configure provider, credentials, alerting and compliance review    |
| Production-scale 100k booking/shift benchmark  | Sprint debt / release blocker | BA approved deferral from Sprint 4 closure; local smoke is not production-scale                                                                                                                                            | Owner: API/Performance Lead; environment: production-like staging; deadline: before production go/no-go; capture p95/p99, CPU, RAM and pool metrics           |
| Safe reseed of an already populated database   | Deferred                      | `db:reset` is deterministic; additive seed is not yet idempotent for arbitrary existing data                                                                                                                               | Add non-destructive seed mode                                                                                                                                 |
| Native-device UI automation for Expo apps      | Deferred                      | Mobile API contracts are covered; device UI gestures are not                                                                                                                                                               | Add Maestro/device lane when mobile release testing begins                                                                                                    |
| Long-duration booking reservation soak         | Deferred                      | Concurrent integration tests prove conflict safety; sustained production-scale contention is not yet measured                                                                                                              | Run 100k-appointment and hot-slot contention soak in staging before launch                                                                                    |
| Customer booking multi-service editor polish   | Resolved                      | Booking Web supports select/remove/reorder, real date windows and policy-aware staff selection                                                                                                                             | Monitor usability in acceptance testing; no Sprint 5 scope implied                                                                                            |
| Sprint 5 production-scale operations benchmark | Release blocker               | Local/CI smoke cannot substantiate production p95 at 100k appointments and 300k sessions                                                                                                                                   | Owner: API/Performance Lead; run the specified dataset in production-like staging before go-live                                                              |
| Object-storage provider and image processing   | Release configuration         | Tenant-bound signed upload foundation and disabled-state UX exist; client reports remain `PENDING_UPLOAD`, but the production trusted callback/verification Worker, bucket and malware/image processing are not configured | Owner: Platform/SecOps; configure private S3-compatible storage, trusted checksum/MIME/size verification, lifecycle policy and scanning before enabling media |
| Native mobile device operational E2E           | Deferred                      | API and Expo web smoke cover Staff Today/Owner summary; physical device gestures and camera flow are not automated                                                                                                         | Add Maestro/device and camera-upload lanes before native release                                                                                              |
| Production terminal/card provider              | Release configuration         | Sprint 6 provides cash and permissioned manual external evidence; no real terminal credentials or hardware are configured                                                                                                  | Select provider, complete PCI/security review, implement signed adapter/webhook and certification before enabling terminal mode                               |
| Receipt printer and delivery providers         | Release configuration         | Immutable print view and background delivery records exist; physical printer, SMS and email production providers remain disabled                                                                                           | Configure printer bridge and approved providers with retry/alerting before go-live                                                                            |
| Country tax/e-invoice integration              | Deferred                      | Deterministic tax snapshots exist but filing and country-specific e-invoice submission are intentionally outside Sprint 6                                                                                                  | Product/Legal selects jurisdictions and approves a separately scoped integration                                                                              |
| Sprint 6 production-scale benchmark            | Release blocker               | Local/CI smoke cannot prove the specified 100k-order/200k-payment p95 targets                                                                                                                                              | Run production-like staging benchmark and hot-order/payment contention soak before production go/no-go                                                        |
| Production refund provider adapter             | Release configuration         | Sprint 7 supports manual external evidence and a deterministic test seam; a certified provider refund/query/webhook adapter is not configured                                                                              | Select the terminal provider, implement signed refund callbacks and complete PCI/security certification before enabling provider execution                    |
| Financial export object storage worker         | Release configuration         | Export jobs and permission/audit boundaries exist; production CSV generation, formula escaping, private object storage and signed download delivery require the selected provider                                          | Configure the private bucket and Worker, add expiry/deletion alerting, and verify CSV-injection tests before go-live                                          |
| Refund high-risk gateway rate limits           | Release configuration         | Application authorization, idempotency and dual control exist, but deployment-specific throttles cannot be proven locally                                                                                                  | Configure per-tenant/user/device limits for approval, execution and provider-result commands in the production gateway                                        |
| Sprint 7 production-scale benchmark            | Release blocker               | Local capacity tests cannot prove 50k-refund/500k-commission-entry production p95 or hot-counter/period-lock behavior                                                                                                      | Run the documented Sprint 7 dataset and contention scenarios in production-like staging before production go/no-go                                            |
| Production voucher HMAC key management         | Release configuration         | Development has a fail-safe default, while production correctly refuses that default                                                                                                                                       | SecOps provisions and rotates `VOUCHER_HMAC_SECRET` in the approved secret manager before enabling vouchers                                                   |
| Sprint 8 production-scale benchmark            | Release blocker               | Local/CI smoke and 20-way contention tests do not prove the specified production-scale benefit wallet, liability and hot-code p95                                                                                          | Run the documented Sprint 8 dataset, hot voucher/points/package contention and long-duration Worker soak in production-like staging before go-live            |
| Benefit exports and liability snapshots        | Release configuration         | Export metadata and append-only snapshot schema exist, but production object generation, private delivery and scheduled accounting reconciliation are not enabled                                                          | Configure the private export Worker/storage and accounting sign-off before enabling production exports                                                        |
| Benefit anomaly detection and gateway limits   | Release configuration         | Domain limits, dual control and audit exist; environment-specific velocity rules and alert destinations cannot be proven locally                                                                                           | Configure per-tenant/code/user/device limits and alerts in the production gateway/SIEM                                                                        |
| Package partial-line unit restoration policy   | Product decision              | Sprint 8 restores whole units after a fully refunded covered line; a partial covered-line refund is preserved as `MANUAL_REVIEW` because fractional units are not approved in the SRS                                      | BA/Product Owner must approve a separate change request before fractional package-unit restoration is automated                                               |

# Sprint 3 carried risks

- Extend the full Sprint 3 capacity benchmark from the current local/CI smoke window to a long-duration production-like soak before launch; the required p95 dataset targets pass locally, but long-run cache-hit and CPU trends still need staging evidence.
- Replace the conservative tenant-wide PostgreSQL version bump with branch-specific event routing only after production cache-hit metrics justify the complexity.
- Add a production operations UI for failed outbox rows; Sprint 3 provides structured metrics/logs and the repository-level manual retry method.
- Persist metrics in the selected production telemetry backend; the current implementation emits structured Nest logs and in-process counters.

# Sprint 9 carry-forward

- Production object-storage signing for private inventory exports remains provider configuration work; exports fail closed without it.
- Production-scale performance evidence requires staging-like data; local Docker figures are not a production claim.
- Automated reorder proposals and forecasting are intentionally deferred beyond Sprint 9 and may not bypass approval controls.

# Sprint 10 carry-forward

- `STORED_VALUE_HMAC_SECRET` needs production secret-manager provisioning, rotation rehearsal and gateway rate limits before stored value is enabled outside approved tenants.
- Email/SMS gift-card delivery stays fail-safe until approved providers and destination encryption are configured; print requests work without exposing persisted plaintext credentials.
- Export jobs currently produce private storage keys as a provider seam; production CSV generation, formula escaping, signed downloads and lifecycle deletion remain release configuration.
- Country-specific expiration, dormancy and breakage remain disabled until Legal approves a versioned jurisdiction policy. Safe default is no expiration and no breakage recognition.
- Local Docker/CI smoke is not a production-scale claim. Run the specified million-ledger/hot-card benchmark and Worker soak on production-like staging before go-live.
- Customer Credit refund destination selection is available only through audited refund allocation workflow; broader self-service conversion or transferable credit requires a Change Request.
- Repository-wide format check includes pre-existing files outside Sprint 10; changed-file whitespace validation passes. Resolve this in a separately scoped formatting-only change to avoid mixing mechanical churn with domain implementation.

# Sprint 11 carry-forward

- Production email provider selection, credentials, webhook signature verification, bounce/complaint ingestion and deliverability monitoring are release configuration. Local/CI `FAKE` mode records `SENT` only and never fabricates `DELIVERED`.
- Production secret-manager provisioning and rotation rehearsal are required for `PUBLIC_ENGAGEMENT_TOKEN_SECRET`; the development fallback is forbidden in production.
- Customer engagement exports have job metadata only. Private object generation, CSV formula escaping, signed download expiry and deletion policy must be configured before release.
- Local campaign/recovery capacity evidence is not a production claim. Run the documented 10k-audience, hot-withdrawal and Worker retry soak on production-like staging.
- SMS, advanced marketing automation, CRM/CDP enrichment, public review syndication and AI are intentionally outside Sprint 11 and require separately authorized scope.
- A provider call that has already begun before a consent-withdrawal commit cannot be recalled transactionally. The lease/preflight boundary prevents every call not yet started and records truthful provider evidence after the boundary; production adapters must support provider-side suppression/webhook reconciliation where available.
- The closure intentionally narrows segment filters to `branchVisited`, `locale`, `tagId`, contactable and marketing-consent semantics. Service/category/spend/tier/recovery/recent-campaign filters require separately tested query plans before they may be advertised.

# Sprint 12 carry-forward

- Production payroll and payout are disabled until a qualified reviewer activates a versioned jurisdiction policy. No default seed value represents actual labor or tax law.
- Production payroll provider/bank adapter, signed webhook verification, credential rotation and provider sandbox certification remain release configuration; missing configuration fails closed and never fabricates `PAID`.
- Tax is an approved withholding input only. Jurisdiction-specific calculation, filing and statutory forms require a separate Change Request and legal sign-off.
- Payroll export metadata/private keys exist; production object rendering, CSV injection test corpus, signed URL expiry and deletion/retention controls must be verified with the selected object store.
- Local deterministic load/concurrency evidence is not a production claim. Run high-volume clock, payroll source and provider-event soak on production-like staging before go-live.
- Kiosk PIN/gateway throttling and device-secret rotation depend on deployment gateway/secret manager; domain device trust and lockout fields are present.
- Commission and tip source types are uniquely reserved in the payroll allocation ledger; end-to-end production settlement import needs final finance reconciliation against provider/commission period calendars.
- Historical tip allocations are intentionally backfilled `UNKNOWN` and excluded from payroll. Finance must attach payout evidence and explicitly classify them before any recovery/import; the migration does not infer whether cash was already handed directly to staff.
- Overtime thresholds remain jurisdiction data. Production enablement requires Legal-approved daily, weekly and consecutive-day rule JSON plus boundary fixtures for every activated jurisdiction; the deterministic engine supplies no statutory default.

# Sprint 13 carry-forward

- Production payment provider credentials, customer/payment-method adapters and signed webhook verification are absent; production fails closed until certification.
- Production tax calculation/filing and revenue recognition/general ledger remain outside scope.
- Platform invoice/export rendering, private signed URLs and retention deletion require the selected object-storage adapter.
- Break-glass remains disabled pending two platform approvers, tenant notification, incident integration and post-incident review automation.
- Local Docker/CI load smoke is not a production claim; run provider-loss, million-event usage and renewal/dunning soak on production-like staging.
