import pg from'pg';import{execFileSync}from'node:child_process';const u=process.env.DATABASE_URL??'postgresql://nailsoft:nailsoft@localhost:5432/nailsoft';const c=new pg.Client({connectionString:u});await c.connect();await c.query('DROP SCHEMA public CASCADE;CREATE SCHEMA public');await c.end();for(const s of['migrate.mjs','seed.mjs'])execFileSync(process.execPath,[`scripts/${s}`],{stdio:'inherit',env:{...process.env,DATABASE_URL:u}});
