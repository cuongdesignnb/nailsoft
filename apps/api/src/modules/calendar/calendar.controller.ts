import{Controller,Get,Inject,Query,Req,UseGuards}from"@nestjs/common";import{ApiBearerAuth,ApiTags}from"@nestjs/swagger";import{AuthGuard}from"../identity/auth.guard.js";import{PermissionGuard}from"../identity/permission.guard.js";import{RequireAnyPermission}from"../identity/permission.decorator.js";import type{AuthenticatedRequest}from"../identity/auth.types.js";import{CalendarService}from"./calendar.service.js";@ApiTags("calendar")@ApiBearerAuth()@UseGuards(AuthGuard,PermissionGuard)@Controller("calendar")export class CalendarController{constructor(@Inject(CalendarService)private readonly service:CalendarService){}private async ok(data:unknown,r:AuthenticatedRequest){return{success:true,data:await data,meta:{requestId:r.raw.requestId??"unknown",timestamp:new Date().toISOString()}};}@Get("events")@RequireAnyPermission("calendar.read_branch","calendar.read_own")events(@Query()q:unknown,@Req()r:AuthenticatedRequest){return this.ok(this.service.events(r.auth,q),r);}@Get("summary")@RequireAnyPermission("calendar.read_branch","calendar.read_own")summary(@Query()q:unknown,@Req()r:AuthenticatedRequest){return this.ok(this.service.summary(r.auth,q),r);}}
